class Solution:
    def sumOfSeries(self,n):
        #code here
        sum = 0
        my_num = n
        if n < 1 or n > 200:
            return 0
            
        while my_num >= 1:
            sum += (my_num ** 3)
            my_num -= 1
            
        return sum
